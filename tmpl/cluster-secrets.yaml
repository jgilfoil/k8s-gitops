# yamllint disable
apiVersion: v1
kind: Secret
metadata:
    name: cluster-secrets
    namespace: flux-system
stringData:
    SECRET_DOMAIN: ${BOOTSTRAP_DOMAIN}
    SECRET_CLOUDDNS_EMAIL: ${BOOTSTRAP_CLOUDDNS_EMAIL}
    SECRET_CLOUDDNS_PROJECT: ${BOOTSTRAP_CLOUDDNS_PROJECT}
